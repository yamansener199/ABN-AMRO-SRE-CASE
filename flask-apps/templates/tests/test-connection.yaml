apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "flask-apps.fullname" . }}-test-connection"
  labels:
    {{- include "flask-apps.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "flask-apps.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
